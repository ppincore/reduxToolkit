import './App.css'
import { useAppSelector } from './store/hooks/redux'

function App() {
  const {} =useAppSelector(state => state.userReducer.)
  return (
    <div>
      цщкв
    </div>
  )
}

export default App
